# Notes: Initialize Spring Boot Project

- Using Spring Boot 3.3.x (latest stable).
- Using Java 21 (LTS).
- Maven structure preferred.
- Initial dependencies selected to cover the upcoming Phase 1 requirements (PostgreSQL, Security, JWT).
- Will use a simple `MainApplication` and a basic `HealthController` for verification.
