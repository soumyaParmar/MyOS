# Notes: Setup PostgreSQL Configuration

- Using Docker Compose for local database management.
- Version: PostgreSQL 16-alpine.
- Environment variable overrides provided in `application.yml`.
- JDBC URL: `jdbc:postgresql://localhost:5432/myos`
